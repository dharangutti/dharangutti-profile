:root{
  --bg:#0f1724;
  --card:#0b1220;
  --accent:#6dd3ff;
  --muted:#9aa7b2;
  --surface:#0b1220;
  --glass: rgba(255,255,255,0.03);
  --max-width:1100px;
  --radius:12px;
  --gap:18px;
  --text:#e6eef6;
}

*{box-sizing:border-box}
html,body{height:100%;margin:0;background:linear-gradient(180deg,#071029 0%, #071829 60%);color:var(--text);font-family:Inter, ui-sans-serif, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;}

.container{
  max-width:var(--max-width);
  margin:28px auto;
  padding:22px;
  background:linear-gradient(180deg, rgba(255,255,255,0.02), rgba(255,255,255,0.01));
  border-radius:14px;
  box-shadow: 0 6px 30px rgba(2,6,23,0.6);
  min-height:60vh;
}

/* Corridor-aware: center column with comfortable reading width */
.header{max-width:820px;margin:0 auto 18px auto;text-align:center}
.header h1{margin:0;font-size:1.8rem}
.subtitle{color:var(--muted);margin-top:6px}

/* Controls */
.controls{display:flex;gap:10px;justify-content:center;align-items:center;margin-top:14px}
.btn{background:var(--accent);border:none;color:#06202a;padding:8px 12px;border-radius:8px;cursor:pointer;font-weight:600}
.btn.secondary{background:transparent;border:1px solid rgba(255,255,255,0.06);color:var(--text)}
.btn.small{padding:6px 10px;font-size:.9rem}
.key-indicator{margin-left:8px;color:var(--muted);font-size:.9rem}

/* Grid of event cards */
.calendar-root{display:grid;grid-template-columns:repeat(auto-fit,minmax(240px,1fr));gap:var(--gap);margin-top:22px;padding:6px}
.event-card{background:linear-gradient(180deg, rgba(255,255,255,0.02), rgba(255,255,255,0.01));padding:16px;border-radius:10px;border:1px solid rgba(255,255,255,0.02);min-height:140px;display:flex;flex-direction:column;justify-content:space-between}
.event-title{font-size:1.05rem;font-weight:700;margin-bottom:6px}
.event-meta{color:var(--muted);font-size:.95rem;margin-bottom:8px}
.event-actions{display:flex;gap:8px;align-items:center}
.badge{background:rgba(255,255,255,0.04);padding:6px 8px;border-radius:8px;color:var(--muted);font-size:0.85rem}

/* Modal */
.modal{position:fixed;inset:0;display:none;align-items:center;justify-content:center;background:linear-gradient(rgba(1,4,8,0.6), rgba(1,4,8,0.76));padding:20px;z-index:1200}
.modal[aria-hidden="false"]{display:flex}
.modal-panel{background:linear-gradient(180deg,#071826 0%, #052033 100%);border-radius:12px;padding:18px;max-width:760px;width:100%;color:var(--text);box-shadow:0 20px 60px rgba(0,0,0,0.6);border:1px solid rgba(255,255,255,0.03)}
.modal-close{position:absolute;right:26px;top:22px;background:transparent;border:none;color:var(--muted);font-size:20px;cursor:pointer}
.modal-body{margin-top:8px;color:var(--text);line-height:1.5;white-space:pre-wrap}
.modal-footer{display:flex;gap:8px;justify-content:flex-end;margin-top:12px}

/* small screens */
@media (max-width:640px){
  .container{padding:16px;margin:8px}
  .header h1{font-size:1.4rem}
  .controls{flex-direction:column;gap:8px}
}
